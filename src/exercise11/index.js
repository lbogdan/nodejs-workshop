// Using the function from exercise 10, write a simple http server
// that returns the n-th decimal of PI on the `/api/pi/:n` endpoint.
// Compare how it works vs. a synchronous function,
// in case of parallel requests.
