// Write a simple http server that serves files from a folder.
// Use `fs.stat()` to check if the file exists
// and set the `Content-Length` header, and a MIME package
// to properly set the `Content-Type` response header.
