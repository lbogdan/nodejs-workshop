// Same as exercise 2, but use util.promisify() to transform
// the callback method `readFile()` into returning a Promise.
// Use `async` / `await` and an IIFE to work around
// the non-supported top-level `await`.
// Bonus points for reading the files in parallel.

// Bonus hint: Promise.allSettled().
